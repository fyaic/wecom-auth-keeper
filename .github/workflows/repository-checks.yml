name: Repository checks

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    name: Syntax and repository structure (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python3 scripts/check_repository.py
      - name: Install native adapter dependencies (macOS only)
        if: runner.os == 'macOS'
        run: python3 -m pip install -r requirements-macos.txt
      - run: python3 -m unittest discover -s tests -v
      - run: git diff --check
